name: Security Checks

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  dependency-review:
    uses: Cosmos-SincoERP/.github/.github/workflows/_reusable-dependency-review.yml@v1

  secret-scan:
    uses: Cosmos-SincoERP/.github/.github/workflows/_reusable-secret-scan.yml@v1
